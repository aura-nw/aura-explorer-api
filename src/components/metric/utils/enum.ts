export enum TypeDate {
  month = 'month',
  day = 'day',
  hour = 'hour',
  minute = 'minute',
}

export enum Range {
  month = '12M',
  day = '30d',
  hour = '24h',
  minute = '60m',
}

export enum RangeType {
  month = 'mo',
  day = 'd',
  hour = 'h',
  minute = 'm',
}

export enum FormatDate {
  month = 'YYYY-MM-01T00:00:00.000',
  day = 'YYYY-MM-DDT00:00:00.000',
  hour = 'YYYY-MM-DDTHH:00:00.000',
  minute = 'YYYY-MM-DDTHH:mm:00.000',
}
