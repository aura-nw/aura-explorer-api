export enum TypeDate {
  month = 'month',
  day = 'day',
  hour = 'hour',
  minute = 'minute',
}

export enum Range {
  month = '12M',
  day = '30d',
  hour = '24h',
  minute = '60m',
}

export enum CW20MetricType {
  price = 'price',
  volume = 'volume',
}
